setSourceList = []
tempDict = {}
def converToDocument(source,keys):
    if len(source) == 0:
        raise ValueError("List has no Values")
    print(type(keys))
    
    caught = map(tempDict,keys)
   

def setDict(key):
    print(setSourceList)
    setSourceList.append((key,""))
    